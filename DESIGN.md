# 반복 생성되는 문서의 디자인 계약

이 문서는 `camera-hal-sdd` 생성기의 유지보수 기준입니다. 특정 사이트의 HTML을 직접 고쳐서 디자인을 유지하지 않습니다.

최종 적용 대상은 사내 Camera HAL입니다. libcamera는 공개 테스트 대상이며, 예제의 빌드·호스팅 환경을 사내 운영의 필수 조건으로 만들지 않습니다. 구체적인 작업 기준은 [AGENTS.md](AGENTS.md)에 기록합니다.

1. 구조는 `facts.json`에서만 가져옵니다. `src/sdd/diagrams.py`가 Markdown 생성과 사이트 빌드에서 사용하는 공통 Mermaid 규칙입니다. LLM에는 배치나 화살표 생성을 맡기지 않습니다.
2. 프로젝트 차이는 `sdd.yaml`의 `diagrams`와 `site`, `sections.yaml`의 클래스 선택·절별 `diagram` 설정으로 표현합니다. libcamera 네임스페이스를 공통 코드에 하드코딩하지 않습니다.
3. Markdown 생성 시 설명 → 관계 그림 → 클래스 근거 순서를 적용합니다. `sdd:class-diagram` 주석 사이의 블록은 생성기가 관리합니다. 디자인 변경으로 인용 검사 상태나 사람 검토 상태를 승격하지 않습니다.
4. 좌측 탐색·중앙 720px 본문·우측 목차, 한국어 16px/1.8, 흰 배경과 제한된 녹색 강조를 유지합니다. 좁은 화면에서는 메뉴와 목차를 접을 수 있어야 합니다.
5. 그림 클릭/Enter 확대, 요소 이름 검색, 실제 SVG 크기 변경, Escape 닫기와 초점 복원을 유지합니다. 외부 Mermaid 로딩에 실패하면 원문과 실패 이유를 표시합니다.
6. 노드 한도 초과나 서로 다른 커밋의 facts·원고를 조용히 통과시키지 않습니다. 작업을 실패시키고 설정을 나누거나 다시 생성할 이유를 알립니다.
7. 빌드는 임시 디렉터리에서 검증한 뒤 배포 디렉터리에 반영합니다. 직전 manifest가 소유한 오래된 파일만 정리하고, `CNAME` 등 별도 운영 파일을 보존합니다. 수동 수정된 생성 파일은 덮어쓰지 않습니다.
8. 변경할 때는 반복 실행의 동일성, 관계 추가·삭제의 반영, Markdown/SVG 원문의 중복 방지, 링크 유효성, 실패 시 기존 출력 보존 테스트를 통과해야 합니다. CSS/JS 변경은 데스크톱·모바일과 검색·그림 조작도 브라우저에서 확인합니다.

기준은 libcamera 공식 문서의 탐색 방식과 OMM `DESIGN.md`의 reading 규칙입니다. OMM 기준 커밋은 `12a704c603136308b87b26c4081bd6102d42395e`이며, OMM 실행기가 없어도 이 저장소만으로 같은 규칙을 적용할 수 있어야 합니다.
