절: {title}
독자: {reader}

이 절이 답해야 하는 질문:
{answers}

사실 (이 목록 밖의 내용은 쓰지 않습니다):
{facts}

기존 본문 (있으면 구조를 유지하고, 사실이 바뀐 부분만 고칩니다):
{existing}

문장 형식의 예 (사실 목록의 `파일:줄` 을 그대로 복사해서 문장 끝에 붙입니다):
`example::inner::ExampleOwner` 는 요청이 제출된 뒤 결과 콜백까지 버퍼를 소유합니다 `dir/ExampleOwner.h:12`. 버퍼는 `create()` 에서 만들어집니다 `dir/ExampleOwner.cpp:40`.

이름 규칙: 사실 목록에 있는 클래스를 문장에서 가리킬 때는 그 이름을 적습니다.
"구체적인 클래스" 나 "해당 구현" 처럼 이름을 생략한 표현으로 대신하지 않습니다.
네임스페이스를 어디까지 적을지는 신경 쓰지 않아도 됩니다. 앞부분을 줄여 써도 생성기가 사실 목록의 이름으로 되돌립니다.

지시: 위 사실만 근거로 이 절의 본문 문단을 씁니다. 위 규칙, 목록, 표, 제목을 다시 출력하지 않습니다. 표와 다이어그램은 이미 본문 앞에 있으므로 표만으로는 알 수 없는 관계와 순서를 설명합니다. 지금 바로 첫 문단부터 씁니다.

본문:
