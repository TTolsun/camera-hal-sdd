# 코드 변경의 문서 범위 검사

`sdd impact`는 재생성할 문서를 고르는 작업과 함께, 변경 파일의 클래스·함수를 선택하는 문서가 있는지 검사합니다. `watch`에 걸려 문서가 재생성된다는 사실만으로 내용까지 포함된다고 판단하지 않습니다. 공통 구현은 `src/sdd/coverage.py`에 있으며 특정 HAL, 빌드 도구나 게시 서비스에 의존하지 않습니다.

## 실행과 결과

```bash
sdd impact --base <이전-커밋> --head <현재-커밋> --base-facts <이전-facts.json>
```

기존 `--config` 옵션은 하위 명령 앞에 둡니다. 현재 설정의 facts는 `--head`, `--base-facts`는 `--base`와 커밋이 일치해야 합니다. 입력 커밋이 다르면 검사를 중단합니다. 이전 facts를 생략할 수 있지만 같은 파일에서 삭제된 심볼을 놓칠 수 있다는 제한을 출력합니다. 커밋을 바꾸어 extract하기 전에 이전 facts를 별도로 보관하세요.

- `build/impact.json`의 `coverage`에는 항목별 선택 섹션, 이전·현재 근거 위치, 검토 이유, 제외 파일이 기록됩니다.
- `build/impact-review.md`는 LLM 없이 작성하는 검토 보고서입니다. 문서 생성 시 LLM 요약이 누락하더라도 이 기록은 남습니다.
- 기본 실행은 보고서를 남기고 종료 코드 0을 반환합니다. 검토 항목이 있으면 `needs-review`, 없으면 `mapped`입니다. `mapped`는 생성된 설명의 정확성이나 사람의 승인을 뜻하지 않습니다.
- `--fail-on-coverage-gap`을 주면 검토 항목을 저장한 뒤 종료 코드 2를 반환합니다. `sdd run --base ... --fail-on-coverage-gap`에서는 문서·사이트 생성 전에 멈춥니다. 이 옵션을 생략한 `generate --from-impact`는 검토 항목 수를 알리고 기존 범위대로 생성합니다.
- 사람이 검토를 마친 항목은 `sdd accept --finding <장부 키>`로 승인 장부(`sdd/approvals.json`)에 기록합니다. 승인(accepted)된 항목은 관문 판정에서 제외되어, 빌드 파일처럼 영구히 남는 항목 때문에 관문이 늘 빨간색이 되는 것을 막습니다. 보류(`--defer`)는 확인 기록만 남기고 관문을 통과시키지 않습니다. 장부 키와 현재 상태는 `build/impact-review.md`에 항목별로 표시됩니다.

## 검토 대상으로 올리는 조건

| 이유 코드 | 의미 | 검토 방법 |
|---|---|---|
| `entity-outside-document-scope` | 변경 파일의 클래스·함수를 선택하는 자동 문서가 없습니다. | 적절한 절의 `facts.classes` 또는 `facts.functions`를 보완하거나 새 절을 정의합니다. |
| `document-not-scheduled` | 대응 문서가 있지만 영향 분석에서 재생성 대상으로 선택되지 않았습니다. | 해당 절의 `watch`와 `impact_classes` 설정을 확인합니다. |
| `no-extracted-entity` | 변경 파일을 클래스·함수 근거에 연결하지 못했습니다. | 빌드 설정·주석 전용 파일·추출 누락·삭제 파일인지 확인합니다. |

클래스는 prose 또는 per-package 문서의 클래스 선택 규칙, 자유 함수는 prose 문서의 함수 선택 규칙과 대조합니다. 전체 이름과 짧은 이름을 모두 지원합니다. 패키지 요약, 시나리오의 일부 호출, 수동 문서의 파일 참조를 클래스·함수 전체가 설명된 근거로 취급하지 않습니다. 수동 문서의 `evidence_files`는 사람이 재검토할 위치로 안내합니다.

libclang이 익명 구조체·공용체에 붙이는 이름은 검사에서 제외합니다. 그 이름에는 파싱한 기계의 절대 경로와 줄 번호가 들어 있어서(`Ctx::(unnamed struct at /home/…/ipa_context.h:33:2)`) 설정의 어떤 패턴으로도 고를 수 없고, 줄이 밀리면 같은 구조체가 둘로 보입니다. 보고해도 해결할 방법이 없는 항목이며, 그 구조체를 담은 바깥 클래스가 같은 파일을 이미 검사 대상으로 잡습니다. libcamera 두 커밋으로 실측했을 때 검토 항목 10개 중 8개가 이것이었고, 제외한 뒤 2개가 남았습니다.

`source.exclude`에 해당하는 파일은 검사에서 제외하되 목록을 남깁니다. 단순히 경고를 없애기 위해 제외 범위를 넓히지 말고 프로젝트의 실제 분석 대상에 맞추세요. 검토를 마친 개별 항목의 승인·보류는 `sdd accept --finding`이 장부에 사람·시각과 함께 기록하며, 검사 결과 자체를 바꾸지는 않습니다.

## 검사 범위와 한계

변경 파일 안에 선언 또는 메서드·함수 정의가 있는 엔티티를 보수적으로 검사합니다. 실제 변경 줄의 의미를 분석하지 않으므로, 같은 헤더에 있는 변경되지 않은 클래스도 검토 목록에 포함될 수 있습니다. 이전 facts를 제공하면 양쪽 파일과 엔티티를 확인합니다. 이름 변경은 이전·이후 경로를 모두 검사하며, Windows에서도 공백과 한글 경로를 보존합니다.

이 검사는 설정상 문서 범위와 재생성 연결을 확인합니다. 문서 파일의 존재·최신성, LLM 입력 예산으로 생략된 내용, 파싱의 완전성, 메서드별 설명 범위와 문장의 참·거짓을 보장하지 않습니다. 설명 정확성 검사는 별도 후속 작업입니다.

## libcamera 실제 검증

앞서 보존한 두 커밋 `0a881d2` → `279d355`의 facts와 실제 Git diff로 실행했습니다. 기존 세 문서 설정은 그대로 사용했습니다.

- 기존 영향 분석은 세 문서를 모두 선택했지만, 새 `libcamera::ipa::ipu3::algorithms::Lsc`를 설명할 절은 없었습니다. 새 검사는 이를 `needs-review`로 검출했습니다.
- LSC 전용 클래스 선택을 임시 섹션에 추가하면 해당 항목만 해소되고 다른 누락 항목은 남는지 확인했습니다. 실제 문서 설정을 임시 검증 설정으로 교체하지 않았습니다.
- 총 검토 항목은 17개입니다. 이 수는 실제 변경 심볼 수가 아닙니다. 변경 헤더의 주변 선언과 이전·현재 위치가 다른 익명 구조체, 자유 함수, 근거에 연결되지 않은 두 파일을 포함합니다.
- 엄격 옵션은 보고서를 남긴 뒤 종료 코드 2로 종료했습니다. LLM 호출, libcamera 재빌드, 공개 문서 배포 없이 보존된 입력으로 검증했습니다.

[검증 기록](../examples/libcamera/validation/20260921/coverage.json)에 입력 커밋, 변경 파일, 발견 항목과 확인 결과를 남겼습니다. 최종 대상인 사내 Camera HAL의 검증 완료를 뜻하지 않습니다.
