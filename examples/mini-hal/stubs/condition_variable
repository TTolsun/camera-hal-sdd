#pragma once
#include <mutex>
namespace std {
class condition_variable {
public:
    void notify_one() {}
    void notify_all() {}
    template <class Lock, class Pred> void wait(Lock&, Pred p) { while (!p()) {} }
};
}
