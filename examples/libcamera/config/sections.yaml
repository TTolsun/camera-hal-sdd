sections:
- id: overview
  group: For Developers
  title: 시스템 개요
  output: overview.md
  kind: prose
  lead: 카메라 탐색, 요청 생성과 완료의 책임을 구분한 뒤 수정할 패키지를 찾으세요.
  reader: libcamera 코드를 처음 읽는 개발자
  answers:
  - 카메라 탐색과 요청 생성은 누가 담당하는가?
  - 캡처를 시작하기 위한 상태와 API 순서는 무엇인가?
  - 요청 제출과 완료 사이의 비동기 경계는 어디인가?
  facts:
    packages:
    - src/*
    - include/libcamera*
    classes:
    - libcamera::CameraManager
    - libcamera::Camera
    - libcamera::Request
    - libcamera::PipelineHandler
    - libcamera::IPAManager
    class_table: false
    impact_classes: []
    reading_order: queue_request
  routes:
  - q: 수정할 패키지를 찾습니다.
    to: '#패키지별-역할'
  - q: 패키지 사이의 의존 방향을 확인합니다.
    to: '#패키지-사이의-의존'
  - q: 요청 코드를 읽을 시작점을 찾습니다.
    to: '#코드를-처음-읽는-순서'
  - q: 클래스 단위 책임을 확인합니다.
    to: camera-model.md
  watch:
  - src/**
  - include/libcamera/**
  - '**/meson.build'
  - meson.options
  - meson_options.txt
  next:
    title: 핵심 시나리오
    link: scenarios/index.md
  design_topics:
  - id: responsibility
    title: 컴포넌트별 책임
    question: 카메라 탐색과 요청 생성은 누가 담당합니까?
    sources:
    - id: camera
      file: src/libcamera/camera.cpp
      start: The Camera class models a camera
      end: \section camera_operation
      sha256: e77cee469b7c57430e4bd4b0f08493289c46b57549873521c476df388d591dc2
    - &id001
      id: discovery
      file: src/libcamera/camera_manager.cpp
      start: int CameraManager::Private::init()
      end: void CameraManager::Private::cleanup()
      sha256: 3e94e4e80c878774bd0a93076401d4ffb09e141dc8c87ae805eed5cc9349f938
    - &id002
      id: handler
      file: src/libcamera/pipeline_handler.cpp
      start: '\class PipelineHandler

        '
      end: \brief Search and acquire a MediaDevice
      sha256: e05278df0a06cb963fe79f772e62d4560b62df15c1411a6b49e32f7ca84d9e74
    - &id003
      id: create
      file: src/libcamera/camera.cpp
      start: \brief Create a request object for the camera
      end: \brief Patch a control list
      sha256: 67f9f4155bfff91e13d4c04e35fb427f6f76615a4c9d2ce06b4fc6ad5ac33108
    statements:
    - text: '`CameraManager`의 초기화는 `IPAManager`와 장치 열거기를 만들고 파이프라인 팩토리의 매칭을 시작합니다. `PipelineHandler::match()`는 필요한
        미디어 장치를 확보하여 하나 이상의 `Camera`를 만들고 관리자에 등록하는 계약을 갖습니다.'
      evidence:
      - discovery
      - handler
      covers:
      - responsibility
    - text: '`Camera`는 단일 이미지 소스의 스트림 구성과 캡처를 제어하는 공개 객체입니다. 애플리케이션이 `Camera::createRequest()`를 호출하면 `Camera`가 `Request`를
        생성하여 호출자에게 소유권을 넘기고, 파이프라인에는 `registerRequest()`로 등록합니다.'
      evidence:
      - camera
      - create
      covers:
      - responsibility
  - id: discovery
    title: 탐색과 파이프라인 선택
    question: 여러 파이프라인 구현은 어떻게 선택됩니까?
    sources:
    - *id001
    - *id002
    statements:
    - text: 장치 열거기 생성 또는 열거가 실패하면 초기화는 `-ENODEV`를 반환합니다. 성공하면 설정의 `pipelines_match_list`가 있을 때 지정된 팩토리를 그 순서대로 시도하고,
        없으면 등록된 팩토리를 순회합니다.
      evidence:
      - discovery
      covers:
      - discovery
    - text: 각 팩토리는 핸들러 인스턴스를 만들고 `match()`가 실패할 때까지 반복합니다. 성공한 인스턴스는 한 파이프라인에 필요한 장치를 확보합니다. 등록된 IPU3·RKISP1·UVC
        구현이 하나의 캡처 요청에서 차례로 실행된다는 뜻은 아닙니다.
      evidence:
      - discovery
      - handler
      covers:
      - discovery
  - id: lifecycle
    title: 캡처 세션의 상태와 자원
    question: 캡처를 시작하고 끝내기 위한 API 계약은 무엇입니까?
    sources:
    - &id006
      id: state
      file: src/libcamera/camera.cpp
      start: \section camera_operation
      end: \enddot
      sha256: e85796df194551ecbd587ba1a7bb356a1409fa1d606762865e7eb989c92d2f72
    - *id003
    statements:
    - text: 애플리케이션은 카메라를 `acquire()`하고 `configure()`한 뒤 `start()`하여 요청을 제출합니다. `stop()`은 Running에서 Stopping을 거쳐
        Configured로 돌아가는 경로이며, 사용을 마치면 `release()`합니다. 해제 전에는 재구성과 반복 시작·정지가 가능합니다.
      evidence:
      - state
      covers:
      - lifecycle
    - text: '`createRequest()`는 Configured 또는 Running에서 허용됩니다. 반환된 요청은 호출자가 소유하며 완료 처리에서 삭제하거나 `Request::reuse()`로
        초기화하여 다시 사용할 수 있습니다. 상태를 바꾸는 함수 사이의 동기화는 호출자가 담당합니다.'
      evidence:
      - create
      - state
      covers:
      - lifecycle
  - id: request
    title: 요청 제출과 완료
    question: 요청의 입력과 완료 통지는 어떤 경계를 통과합니까?
    sources:
    - &id004
      id: queue
      file: src/libcamera/camera.cpp
      start: \brief Queue a request to the camera
      end: \brief Start capture from camera
      sha256: 96db61baeb32065c966b20942f97518ddbd3a43b36b64e8083eb29a90ecb7f4b
    - id: pipeline-queue
      file: src/libcamera/pipeline_handler.cpp
      start: \fn PipelineHandler::registerRequest()
      end: \brief Complete a buffer for a request
      sha256: 5a7a3397a6034a73fe348ae44eb1e2568d2823e95542fe43386e16359d627180
    - &id005
      id: completion
      file: src/libcamera/pipeline_handler.cpp
      start: \brief Complete a buffer for a request
      end: \brief Retrieve the absolute path to a platform configuration file
      sha256: 514288cc958de9ff5fd19d46393cec3bb3088f4f08486528d4559071138638c9
    statements:
    - text: 애플리케이션은 요청에 버퍼와 컨트롤을 넣습니다. `Camera::queueRequest()`는 Running 상태, 요청이 속한 카메라, 요청 상태, 컨트롤 목록과 활성 스트림을
        확인하고 빈 버퍼 요청을 거절합니다. 검사를 통과하면 `ConnectionTypeQueued`로 파이프라인에 전달하며, 반환값 0은 캡처 완료를 뜻하지 않습니다.
      evidence:
      - queue
      covers:
      - request
    - text: 공통 파이프라인은 요청을 대기 큐에 넣어 준비한 후 장치 큐에 전달합니다. `completeBuffer()`는 버퍼 완료를 알리고, 요청 완료는 별도의 `completeRequest()`가
        처리합니다. 완료된 요청은 제출 순서대로 `Camera::requestComplete()`에 전달됩니다.
      evidence:
      - pipeline-queue
      - completion
      covers:
      - request
  - id: boundary
    title: 스레드 경계와 확인 범위
    question: 코드에서 확인한 호출 경계와 실행 검증 범위는 무엇입니까?
    sources:
    - *id004
    - *id005
    - *id006
    statements:
    - text: '`Camera::queueRequest()`는 API 계약상 threadsafe이며 파이프라인 호출을 큐에 예약합니다. `PipelineHandler::completeRequest()`의
        호출 문맥은 CameraManager 스레드로 명시되어 있습니다. 이 계약을 애플리케이션의 모든 콜백에 동일하게 적용해서는 안 됩니다.'
      evidence:
      - queue
      - completion
      covers:
      - boundary
    - text: 실제 신호 수신자의 연결 방식, 재진입, 센서별 지연과 하드웨어 오류 복구는 이 정적 문서로 검증되지 않습니다. 실행 구성과 수신자 코드를 확인하고 기기에서 관찰해야 합니다.
      evidence:
      - queue
      - completion
      kind: limitation
      covers:
      - boundary
  design_requirements:
  - id: responsibility
    question: 카메라 탐색과 요청 생성은 누가 담당합니까?
  - id: discovery
    question: 여러 파이프라인 구현은 어떻게 선택됩니까?
  - id: lifecycle
    question: 캡처를 시작하고 끝내기 위한 API 계약은 무엇입니까?
  - id: request
    question: 요청의 입력과 완료 통지는 어떤 경계를 통과합니까?
  - id: boundary
    question: 코드에서 확인한 호출 경계와 실행 검증 범위는 무엇입니까?
  show_evidence: true
- id: scenarios
  group: For Developers
  title: 핵심 시나리오
  output: scenarios/index.md
  kind: per-scenario
  lead: 추적하려는 동작을 표에서 고르세요. 각 시나리오에서 주요 호출, 추적 경계와 전체 기록을 확인할 수 있습니다.
  reader: 특정 동작의 호출 경로를 추적하는 개발자
  answers:
  - 진입점에서 어떤 컴포넌트의 호출이 추출되었는가?
  - 어디서 분기하고, 어디서 정적 추적이 끊기는가?
  facts:
    scenarios: '*'
  watch:
  - src/libcamera/**
  - src/ipa/**
  next:
    title: 카메라와 요청 모델
    link: camera-model.md
- id: camera-model
  group: For Developers
  title: 카메라와 요청 모델
  output: camera-model.md
  kind: prose
  lead: 클래스의 책임, 요청과 버퍼의 수명, 상태 전이와 종료·동기화 계약을 코드 근거로 확인하세요.
  reader: libcamera의 공개 API를 분석하는 개발자
  answers:
  - 카메라 탐색과 요청 처리는 어떤 클래스에 나뉘어 있는가?
  - 정적으로 확인되지 않는 실행 경계는 어디인가?
  facts:
    classes:
    - libcamera::CameraManager
    - libcamera::Camera
    - libcamera::Request
  watch:
  - include/libcamera/**
  - src/libcamera/camera*.cpp
  - src/libcamera/request.cpp
  - src/libcamera/pipeline_handler.cpp
  - src/libcamera/meson.build
  - include/libcamera/meson.build
  - meson.options
  - meson_options.txt
  next:
    title: Pipeline Handler
    link: pipeline-handler.md
  semantic_review: true
  design_topics:
  - id: responsibility
    title: 클래스별 책임
    question: CameraManager, Camera, Request의 책임과 객체 수명은 어떻게 나뉩니까?
    sources:
    - id: manager
      file: src/libcamera/camera_manager.cpp
      start: \class CameraManager
      end: CameraManager *CameraManager::self_
      sha256: adcd6d49b0c989ec78a81cacece7f6c871a8e0ddf3aa52e42648fae2760a6e84
    - id: camera
      file: src/libcamera/camera.cpp
      start: The Camera class models a camera
      end: \section camera_operation
      sha256: e77cee469b7c57430e4bd4b0f08493289c46b57549873521c476df388d591dc2
    - id: request
      file: src/libcamera/request.cpp
      start: '\class Request

        '
      end: Request::Request(Camera *camera, uint64_t cookie)
      sha256: 948968e35cb0d98957bc63aa2d6b613d2a8203e66855db23974f035e9d679764
    statements:
    - text: CameraManager는 장치를 열거하고 파이프라인과 연결하여 애플리케이션에 카메라를 제공합니다. 동시에 한 인스턴스만 존재해야 하며, 카메라 참조를 모두 해제한 뒤 관리자를 정지해야
        합니다.
      evidence:
      - manager
      covers:
      - responsibility
    - text: Camera는 단일 이미지 소스의 스트림 구성과 캡처를 제어합니다. create()가 반환하는 shared_ptr로 수명을 관리하며 생성자와 소멸자는 비공개입니다.
      evidence:
      - camera
      covers:
      - responsibility
    - text: Request는 프레임별 버퍼와 컨트롤을 묶는 캡처 요청입니다. cookie는 완료 처리에서 외부 자원과 요청을 연결할 수 있도록 애플리케이션이 지정하는 값입니다.
      evidence:
      - request
      covers:
      - responsibility
  - id: state
    title: 상태와 허용 호출
    question: 상태 전이와 허용 호출을 설명하고, 상태를 바꾸는 함수의 동기화 책임을 구분하세요.
    sources:
    - id: states
      file: src/libcamera/camera.cpp
      start: \section camera_operation
      end: \enddot
      sha256: e85796df194551ecbd587ba1a7bb356a1409fa1d606762865e7eb989c92d2f72
    - &id010
      id: start
      file: src/libcamera/camera.cpp
      start: \brief Start capture from camera
      end: int Camera::start(const ControlList *controls)
      sha256: e0abda64dc6b1bdd78d1326c8dd8651e842c03181dcf28e3b8b7eac1a4c70fcf
    statements:
    - text: 일반적인 순서는 Available → acquire() → Acquired → configure() → Configured → start() → Running입니다. Configured에서는
        재구성과 요청 생성이 가능하고, Running에서는 요청 생성과 큐잉이 가능합니다.
      evidence:
      - states
      covers:
      - state
    - text: Running에서 stop()을 호출하면 Stopping을 거쳐 Configured로 돌아갑니다. Acquired 또는 Configured에서 release()를 호출하면 Available로
        돌아갑니다. 현재 상태에서 허용되지 않은 연산의 동작은 정의되지 않으며, 상태도에 아예 없는 연산은 모든 상태에서 허용됩니다.
      evidence:
      - states
      covers:
      - state
    - text: start()는 Configured에서만 호출하며, 호출자가 다른 상태 변경 함수와 동기화해야 합니다. 상태 변경 함수 전반의 동기화를 Camera가 대신 보장하지 않습니다.
      evidence:
      - states
      - start
      covers:
      - state
  - id: request
    title: 요청 생성과 완료
    question: 요청 생성, 버퍼 연결, 큐잉, 완료 통지의 순서와 제출 조건을 설명하세요.
    sources:
    - &id007
      id: create
      file: src/libcamera/camera.cpp
      start: \brief Create a request object for the camera
      end: std::unique_ptr<Request> Camera::createRequest(uint64_t cookie)
      sha256: 9f251ecf5ba569606e9753d3619b88e1aef5a4ce15f816ae97efd283e34b823b
    - &id009
      id: queue
      file: src/libcamera/camera.cpp
      start: \brief Queue a request to the camera
      end: \brief Start capture from camera
      sha256: 96db61baeb32065c966b20942f97518ddbd3a43b36b64e8083eb29a90ecb7f4b
    - &id008
      id: buffer
      file: src/libcamera/request.cpp
      start: \brief Add a FrameBuffer with its associated Stream to the Request
      end: int Request::addBuffer(const Stream *stream, FrameBuffer *buffer,
      sha256: 65cd8b0501b7f6545dd10eaaec4a4fe46c52a096a3bbd94ea6b4813e4a670876
    statements:
    - text: Configured 또는 Running 상태에서 createRequest()로 빈 요청을 만들고 addBuffer()로 스트림과 버퍼를 연결합니다. 요청에는 최소 하나의 캡처 버퍼가
        있어야 합니다.
      evidence:
      - create
      - queue
      - buffer
      covers:
      - request
    - text: Running 상태에서 queueRequest()를 호출하여 제출합니다. 완료 시 requestCompleted 신호를 받으며, 요청 소유자인 호출자는 완료 핸들러에서 삭제하거나
        reuse()로 초기화한 뒤 다시 사용할 수 있습니다.
      evidence:
      - queue
      - create
      covers:
      - request
    - text: '`queueRequest()`는 검사를 통과한 요청을 `ConnectionTypeQueued`로 PipelineHandler에 전달합니다. 반환값 0은 이 큐잉을 마쳤다는 뜻이며,
        장치 제출이나 캡처 완료를 보장하지 않습니다.'
      evidence:
      - queue
      covers:
      - request
    - text: 유효한 fence를 addBuffer()에 전달하면 성공한 경우에만 fence가 버퍼로 이동합니다. 모든 버퍼의 fence가 신호를 보내야 장치에 요청을 큐잉할 수 있습니다.
      evidence:
      - buffer
      covers:
      - request
  - id: lifetime
    title: 소유권과 재사용
    question: 요청 소유권과 완료 뒤 재사용, ReuseBuffers, fence 이전 조건을 설명하세요. 근거에 없는 버퍼 소유권은 추정하지 마세요.
    sources:
    - *id007
    - id: reuse
      file: src/libcamera/request.cpp
      start: \brief Reset the request for reuse
      end: \fn Request::controls()
      sha256: 22294a6809bd4c6655824af9603fb3879603cec256c6845e00d3d2a0e6bf7679
    - *id008
    statements:
    - text: createRequest()가 반환한 요청의 소유권은 호출자에게 있습니다. 요청을 완료 핸들러에서 삭제하거나 Request::reuse()로 초기화하여 재사용합니다.
      evidence:
      - create
      covers:
      - lifetime
    - text: '`addBuffer()`는 버퍼의 참조를 요청에 저장합니다. 호출자는 요청 완료 콜백이 호출될 때까지 해당 버퍼가 유효하도록 보장해야 합니다.
        스트림 하나에 버퍼 하나만 연결할 수 있으며, 같은 스트림에 이미 버퍼가 있거나 버퍼에 fence가 남아 있으면 `-EEXIST`를 반환합니다.'
      evidence:
      - buffer
      covers:
      - lifetime
    - text: reuse()는 요청 상태와 컨트롤·메타데이터를 초기화합니다. ReuseBuffers를 지정하면 기존 버퍼 연결을 재사용하고, 지정하지 않으면 버퍼 맵을 비웁니다. 새 요청 대신
        재사용하는 경우 재큐잉 전에 호출합니다.
      evidence:
      - reuse
      covers:
      - lifetime
    - text: fence의 소유권은 addBuffer() 성공 시 버퍼로 이동합니다. 신호를 받지 못해 타임아웃된 fence는 버퍼에 남으므로, 다른 요청에 버퍼를 재사용하기 전에 releaseFence()로
        꺼내야 합니다. 실패한 addBuffer() 호출은 전달된 fence를 변경하지 않습니다.
      evidence:
      - buffer
      covers:
      - lifetime
  - id: shutdown
    title: 오류와 종료
    question: queueRequest의 오류 조건과 stop의 취소·완료·복귀 상태를 설명하세요.
    sources:
    - *id009
    - &id011
      id: stop
      file: src/libcamera/camera.cpp
      start: \brief Stop capture from camera
      end: \brief Handle request completion and notify application
      sha256: 0009c665993b995489738e48affc81643a7159d82b5326e5f182e30fe11e4901
    statements:
    - text: queueRequest()는 연결이 끊긴 카메라에 -ENODEV, 실행 중이 아닌 카메라에 -EACCES, 다른 카메라의 요청에 -EXDEV를 반환합니다. 유효하지 않은 요청에는
        -EINVAL, 처리할 버퍼 메모리가 부족하면 -ENOMEM을 반환한다고 API가 명시합니다. 빈 버퍼 요청은 큐잉하지 않습니다.
      evidence:
      - queue
      covers:
      - shutdown
    - text: stop()은 대기 중인 요청을 오류 상태로 취소하여 동기적으로 완료합니다. 구현은 실행 중이 아니면 즉시 0을 반환하므로, 이 경로가 -EACCES를 반환한다고 해석하면 안 됩니다.
      evidence:
      - stop
      covers:
      - shutdown
    - text: 실행 중인 카메라는 CameraStopping으로 전환한 뒤 ConnectionTypeBlocking으로 PipelineHandler::stop을 호출합니다. 대기 요청이 없음을
        확인하고 CameraConfigured로 돌아갑니다. 하드웨어별 자원 해제 완료는 별도의 실행 검증이 필요합니다.
      evidence:
      - stop
      covers:
      - shutdown
  - id: concurrency
    title: 동시성과 실행 확인
    question: threadsafe 보장, 호출자의 동기화 책임과 파이프라인의 완료 통지 계약을 구분하세요. 애플리케이션 콜백 전달 방식과 실기기 검증은 별도로 확인하세요.
    sources:
    - *id007
    - *id009
    - *id010
    - *id011
    - id: completion
      file: src/libcamera/pipeline_handler.cpp
      start: \brief Complete a buffer for a request
      end: \brief Retrieve the absolute path to a platform configuration file
      sha256: 514288cc958de9ff5fd19d46393cec3bb3088f4f08486528d4559071138638c9
    statements:
    - text: createRequest()와 queueRequest()는 API 주석에 threadsafe로 명시되어 있습니다. 허용 상태는 각각 Configured 또는 Running과 Running이며,
        이 보장을 모든 Request 메서드로 확대하지 않습니다.
      evidence:
      - create
      - queue
      covers:
      - concurrency
    - text: start()와 stop()을 다른 상태 변경 함수와 동기화하는 책임은 호출자에게 있습니다. stop() 내부의 파이프라인 호출은 ConnectionTypeBlocking이며 비동기
        호출로 설명하면 안 됩니다.
      evidence:
      - start
      - stop
      covers:
      - concurrency
    - text: '확인 필요: 이 발췌만으로 requestCompleted 콜백의 실제 실행 스레드, 재진입 가능성, 하드웨어별 종료 시점은 확정하지 않습니다. 해당 연결 방식과 파이프라인 구현을
        추가로 검토하고 실행으로 확인해야 합니다.'
      evidence:
      - queue
      - stop
      covers:
      - concurrency
      kind: limitation
    - text: '`PipelineHandler::completeRequest()`는 CameraManager 스레드에서 호출해야 합니다. 요청은 애플리케이션에 제출 순서대로 반환되며,
        함수가 돌아온 뒤 파이프라인은 해당 요청에 접근해서는 안 됩니다. 이 호출 문맥을 모든 애플리케이션 콜백의 실행 스레드 보장으로 확대하지 않습니다.'
      evidence:
      - completion
      covers:
      - concurrency
  design_requirements:
  - id: responsibility
    question: CameraManager, Camera, Request의 책임과 객체 수명은 어떻게 나뉩니까?
  - id: state
    question: 상태 전이와 허용 호출을 설명하고, 상태를 바꾸는 함수의 동기화 책임을 구분하세요.
  - id: request
    question: 요청 생성, 버퍼 연결, 큐잉, 완료 통지의 순서와 제출 조건을 설명하세요.
  - id: lifetime
    question: 요청 소유권과 완료 뒤 재사용, ReuseBuffers, fence 이전 조건을 설명하세요. 근거에 없는 버퍼 소유권은 추정하지 마세요.
  - id: shutdown
    question: queueRequest의 오류 조건과 stop의 취소·완료·복귀 상태를 설명하세요.
  - id: concurrency
    question: threadsafe 보장, 호출자의 동기화 책임과 파이프라인의 완료 통지 계약을 구분하세요. 애플리케이션 콜백 전달 방식과 실기기 검증은 별도로 확인하세요.
  show_evidence: true
- id: pipeline-handler
  group: For Vendors
  title: Pipeline Handler
  output: pipeline-handler.md
  kind: prose
  lead: 파이프라인 구현이 지켜야 하는 구성·요청·완료·종료 계약을 확인하세요.
  reader: 파이프라인 구현을 수정하는 개발자
  answers:
  - 공통 클래스와 하드웨어별 구현의 상속 관계는 무엇인가?
  - 설정과 요청 전달에서 직접 확인할 수 있는 메서드는 무엇인가?
  facts:
    classes:
    - libcamera::PipelineHandler
    - libcamera::PipelineHandler*
  watch:
  - src/libcamera/pipeline/**
  - src/libcamera/pipeline_handler.cpp
  - include/libcamera/internal/pipeline_handler.h
  - src/libcamera/pipeline/**/meson.build
  - meson.options
  - meson_options.txt
  next:
    title: IPA 관련 클래스
    link: ipa.md
  semantic_review: true
  design_topics:
  - id: responsibility
    title: 장치 매칭과 객체 수명
    question: 핸들러는 어떤 객체와 장치를 만들고 관리합니까?
    sources:
    - *id002
    - id: acquisition
      file: src/libcamera/pipeline_handler.cpp
      start: \brief Search and acquire a MediaDevice
      end: \fn PipelineHandler::generateConfiguration()
      sha256: 379b17c52a1e97055bf78aaabd964595fb3ec506c568d112e552ed1c29235591
    statements:
    - text: '`PipelineHandler::match()`는 한 파이프라인의 미디어 장치를 확보하여 카메라를 만들고 등록합니다. 필요한 장치를 모두 얻지 못하면 확보한 장치를 해제하고 false를
        반환해야 합니다. 핸들러 인스턴스는 `PipelineHandlerFactoryBase::create()`를 통해 생성하며 공유 포인터로 수명을 관리합니다.'
      evidence:
      - handler
      covers:
      - responsibility
    - text: '`acquireMediaDevice()`로 얻은 장치는 핸들러에 보관되고 핸들러 소멸 시 해제되는 계약입니다. 카메라의 배타 접근을 위한 `acquire()`와 `release()`는
        성공한 횟수가 대응해야 하며, 공유 자원은 모든 관련 카메라의 해제가 끝나기 전에 반납해서는 안 됩니다.'
      evidence:
      - acquisition
      covers:
      - responsibility
  - id: configuration
    title: 구성의 입력과 검증 책임
    question: 기본 구성과 실제 구성에서 호출자와 구현의 책임은 무엇입니까?
    sources:
    - id: configuration
      file: src/libcamera/pipeline_handler.cpp
      start: \fn PipelineHandler::generateConfiguration()
      end: \fn PipelineHandler::exportFrameBuffers()
      sha256: 5284e6062c894a77328b372676c42c1c97a4a01f9b2dccc4d09c83b2578b0e20
    statements:
    - text: '`generateConfiguration()`은 요청한 스트림 역할을 만족하는 기본 구성을 반환하고 불가능하면 null을 반환합니다. 어느 스레드에서 호출해도 안전해야 하며 파이프라인
        안의 카메라 상태를 변경해서는 안 됩니다.'
      evidence:
      - configuration
      covers:
      - configuration
    - text: '`configure()`의 입력은 이미 `CameraConfiguration::validate()`를 통과한 구성입니다. 구현은 각 `StreamConfiguration`에 `setStream()`으로
        스트림을 연결해야 합니다. 호출 문맥은 CameraManager 스레드이며 성공 시 0, 실패 시 음수 오류 코드를 반환합니다.'
      evidence:
      - configuration
      covers:
      - configuration
  - id: queue
    title: 요청 준비와 장치 큐
    question: 요청을 장치로 넘기는 조건과 실패 처리는 무엇입니까?
    sources:
    - id: queue
      file: src/libcamera/pipeline_handler.cpp
      start: \fn PipelineHandler::registerRequest()
      end: \brief Complete a buffer for a request
      sha256: 5a7a3397a6034a73fe348ae44eb1e2568d2823e95542fe43386e16359d627180
    statements:
    - text: '`registerRequest()`는 준비 완료 신호를 `doQueueRequests()`에 연결합니다. `queueRequest()`는 요청을 대기 큐에 넣고 `prepare(300ms)`를
        호출합니다. 대기 큐의 맨 앞 요청이 준비되지 않았거나 장치 큐가 `maxQueuedRequestsDevice_`에 도달하면 추가 전달을 멈춥니다.'
      evidence:
      - queue
      covers:
      - queue
    - text: 전달할 요청은 대기 큐에서 먼저 제거하므로 재귀적으로 `doQueueRequests()`가 호출되어도 같은 요청을 다시 꺼내지 않습니다. `doQueueRequest()`는 장치
        큐에 요청을 등록하고 시퀀스 번호를 부여합니다. 이미 취소된 요청은 완료 처리로 보내고, `queueRequestDevice()`가 오류를 반환하면 취소합니다.
      evidence:
      - queue
      covers:
      - queue
    - text: '`queueRequestDevice()`는 요청에 담긴 스트림별 버퍼와 파라미터를 하드웨어에 적용할 책임을 갖습니다. 호출 문맥은 CameraManager 스레드이며 성공 시 0,
        실패 시 음수 오류 코드를 반환합니다.'
      evidence:
      - queue
      covers:
      - queue
  - id: completion
    title: 버퍼 완료와 요청 반환
    question: 버퍼 완료, 요청 완료와 접근 가능한 수명은 어떻게 구분됩니까?
    sources:
    - *id005
    statements:
    - text: '`completeBuffer()`는 애플리케이션에 버퍼 완료를 알리고 요청 내부의 버퍼 추적 정보를 갱신합니다. 마지막 버퍼가 완료되어도 요청을 자동 완료하지 않으므로 파이프라인이
        필요한 후처리 후 `completeRequest()`를 명시적으로 호출해야 합니다.'
      evidence:
      - completion
      covers:
      - completion
    - text: '`completeRequest()`는 요청을 완료 상태로 바꾸고 장치 큐의 앞에서부터 Pending이 아닌 요청을 반환합니다. 장치가 완료하는 순서와 무관하게 애플리케이션에는 제출
        순서로 돌아갑니다. 반환 가능한 요청을 처리한 뒤 대기 요청의 장치 전달을 재개합니다.'
      evidence:
      - completion
      covers:
      - completion
    - text: 완료 통지 함수는 CameraManager 스레드에서 호출해야 합니다. `completeRequest()`가 돌아온 뒤에는 파이프라인이 해당 요청에 접근해서는 안 됩니다. `cancelRequest()`에도
        같은 수명 규칙이 적용됩니다.
      evidence:
      - completion
      covers:
      - completion
  - id: shutdown
    title: 정지와 미완료 요청
    question: 정지 중 재큐잉을 막고 대기 요청을 반환하는 방법은 무엇입니까?
    sources:
    - id: stop
      file: src/libcamera/pipeline_handler.cpp
      start: \brief Stop capturing from all running streams and cancel pending requests
      end: \brief Determine if the camera has any requests pending
      sha256: 108b1bea5a8314ba57213d15c1e3ba01abe2b3f8a9e0e1e1281864b5492c7304
    - *id005
    statements:
    - text: '`stop()`은 대기 큐를 지역 변수로 옮긴 뒤 `stopDevice()`를 호출합니다. 따라서 장치 정지 중 `completeRequest()`가 호출되어도 기존 대기 요청을
        장치에 다시 넣지 않습니다.'
      evidence:
      - stop
      covers:
      - shutdown
    - text: 장치 정지 후 남은 대기 요청을 취소하고 `doQueueRequest()`로 시퀀스 번호와 완료 순서를 유지합니다. 끝에서는 장치 큐와 대기 큐가 비었음을 확인하고 요청 시퀀스를
        0으로 초기화합니다. 하드웨어 구현의 `stopDevice()`는 미완료 요청을 오류 상태로 완료해야 하는 계약을 갖습니다.
      evidence:
      - stop
      covers:
      - shutdown
    - text: 개별 드라이버가 정지 계약을 지키는지, 완료 신호가 종료와 경합할 때 안전한지는 해당 구현과 기기에서 확인해야 합니다. 공통 클래스의 계약만으로 하드웨어 종료를 검증했다고 판단하지
        않습니다.
      evidence:
      - stop
      - completion
      kind: limitation
      covers:
      - shutdown
  design_requirements:
  - id: responsibility
    question: 핸들러는 어떤 객체와 장치를 만들고 관리합니까?
  - id: configuration
    question: 기본 구성과 실제 구성에서 호출자와 구현의 책임은 무엇입니까?
  - id: queue
    question: 요청을 장치로 넘기는 조건과 실패 처리는 무엇입니까?
  - id: completion
    question: 버퍼 완료, 요청 완료와 접근 가능한 수명은 어떻게 구분됩니까?
  - id: shutdown
    question: 정지 중 재큐잉을 막고 대기 요청을 반환하는 방법은 무엇입니까?
  show_evidence: true
- id: ipa
  group: For Vendors
  title: IPA 관리와 구현 진입점
  output: ipa.md
  kind: prose
  lead: IPA 모듈 선택, 스레드·프로세스 경계와 요청·결과 인터페이스를 구현 근거로 확인하세요.
  reader: 영상 처리 알고리즘과 프로세스 경계를 분석하는 개발자
  answers:
  - 사실에 포함된 IPA 관련 클래스는 무엇인가?
  - 프록시와 알고리즘 사이에서 추가 확인이 필요한 경계는 무엇인가?
  facts:
    classes:
    - libcamera::IPAManager
    - libcamera::IPAModule
    - libcamera::IPAProxy
    - libcamera::ipa::ipu3::IPAIPU3
    - libcamera::ipa::rkisp1::IPARkISP1
  watch:
  - src/ipa/**
  - src/libcamera/ipa*
  - src/libcamera/proxy/**
  - include/libcamera/ipa/**
  - include/libcamera/internal/ipa*
  - src/ipa/**/meson.build
  - src/libcamera/proxy/**/meson.build
  - meson.options
  - meson_options.txt
  - utils/codegen/ipc/**
  - src/libcamera/ipc_pipe*
  next:
    title: IPU3 LSC와 상태 연결
    link: ipu3-lsc.md
  semantic_review: true
  design_topics:
  - id: selection
    title: 모듈 탐색과 격리 조건
    question: 어떤 IPA 모듈과 실행 방식이 선택됩니까?
    sources:
    - id: modules
      file: src/libcamera/ipa_manager.cpp
      start: IPAManager::IPAManager(
      end: IPAManager::~IPAManager() = default;
      sha256: ed1b15d5743b434eb29e34ba981de978ff4dd9bcb186859e4800931a525e766c
    - id: match
      file: src/libcamera/ipa_module.cpp
      start: \brief Verify if the IPA module matches a given name
      end: std::string IPAModule::logPrefix()
      sha256: 99940db9ea353ad0d8282b7c2148578f4c6054d785ef7aaf690d522b52bafb5b
    - &id014
      id: selection
      file: include/libcamera/internal/ipa_manager.h
      start: template<typename T>
      end: '#if HAVE_IPA_PUBKEY'
      sha256: e0bd5192ea24a8b39d3df7b7b93c8b17614663a0144f6dd0741d410fa321d458
    - id: signature
      file: src/libcamera/ipa_manager.cpp
      start: bool IPAManager::isSignatureValid(
      end: '} /* namespace libcamera */'
      sha256: f657ce54df77453510108b02060166edd7506c982d13d3e4868fadee4065a677
    statements:
    - text: '`IPAManager`는 설정의 모듈 경로, 설치 전 빌드 경로, 설치된 시스템 경로 순으로 IPA 후보를 찾습니다. `IPAModule::match()`는 이름의 일치와 최소·최대
        버전을 포함하는 범위를 확인합니다.'
      evidence:
      - modules
      - match
      covers:
      - selection
    - text: 현재 `createIPA<T>()` 구현은 서명 검증이 성공하면 `T::Threaded`, 실패하면 `T::Isolated`를 생성합니다. 공개 키 지원이 없거나 강제 격리 설정이
        켜져 있거나 서명을 검증하지 못하면 격리 경로를 선택합니다. 라이선스 이름만으로 현재의 실행 방식을 판정하지 않습니다.
      evidence:
      - selection
      - signature
      covers:
      - selection
    - text: 일치하는 모듈이 없거나 생성된 프록시의 `isValid()`가 false이면 `createIPA()`는 null을 반환합니다. 호출자는 프록시를 사용하기 전에 이 결과를 확인해야
        합니다.
      evidence:
      - selection
      covers:
      - selection
  - id: execution
    title: 스레드와 프로세스 경계
    question: 동기 호출과 비동기 호출은 어디에서 실행됩니까?
    sources:
    - &id012
      id: threaded
      file: utils/codegen/ipc/generators/libcamera_templates/module_ipa_proxy.cpp.tmpl
      start: '{{proxy_name}}Threaded::{{proxy_name}}Threaded('
      end: /* ========================================================================== */
      sha256: af4b12c81ffee2e9979c01a7e7eb188f5568d258ba03e522b60a3608dc226abd
    - &id013
      id: isolated
      file: utils/codegen/ipc/generators/libcamera_templates/module_ipa_proxy.cpp.tmpl
      start: '{{proxy_name}}Isolated::{{proxy_name}}Isolated('
      end: void {{proxy_name}}Isolated::recvMessage(
      sha256: dd0ba6e352ffacd6c27c6ff5bb447af14838d75ca6aed8d056855ec442a97744
    - id: worker-path
      file: src/libcamera/ipa_proxy.cpp
      start: \brief Find a valid full path for a proxy worker
      end: \var IPAProxy::valid_
      sha256: 8cbcadf7036cdb391f4c48af8562cb637752242f9564f1118c949a62df9dbd77
    - &id016
      id: ipc
      file: src/libcamera/ipc_pipe_unixsocket.cpp
      start: IPCPipeUnixSocket::IPCPipeUnixSocket(
      end: '} /* namespace libcamera */'
      sha256: 0abe488ceae72cca0a5b96c7223ffc4cc412ab18f94765fd7f001dc450544f40
    statements:
    - text: Threaded 프록시는 모듈을 적재하여 IPA 인터페이스를 만들고 소유합니다. `init()` 이후 ThreadProxy를 전용 스레드로 이동시키며, `start()`는 스레드를
        시작하고 Blocking 연결로 호출합니다. 비동기 메서드는 Running 상태에서 Queued 연결로 전달하고, 나머지 동기 메서드는 생성 템플릿의 별도 분기에 따라 직접 호출합니다.
      evidence:
      - threaded
      covers:
      - execution
    - text: Isolated 프록시는 `resolvePath()`로 worker 실행 파일의 경로를 찾고 `IPCPipeUnixSocket`을 생성합니다. 소켓 생성과 `Process::start()`가
        성공해야 연결 상태가 설정됩니다. 프로세스를 시작하는 곳은 IPC 파이프이며 `resolvePath()` 자체는 경로만 반환합니다.
      evidence:
      - isolated
      - worker-path
      - ipc
      covers:
      - execution
    - text: Isolated 프록시의 메서드는 인자를 데이터와 파일 디스크립터로 직렬화하여 전달합니다. 인터페이스의 async 메서드는 `sendAsync()`, 동기 메서드는 `sendSync()`를
        사용합니다. 동기 호출의 출력과 반환값은 응답에서 역직렬화합니다.
      evidence:
      - isolated
      covers:
      - execution
  - id: interface
    title: 프레임 입력과 결과 인터페이스
    question: 파이프라인과 알고리즘은 어떤 입력과 결과를 주고받습니까?
    sources:
    - id: ipu3-api
      file: include/libcamera/ipa/ipu3.mojom
      start: interface IPAIPU3Interface {
      end: 'metadataReady(uint32 frame, libcamera.ControlList metadata);

        };'
      sha256: f1969299336634cdd04e42402648a452126032a25540ad5c78bd2c804362a381
    - id: rkisp1-api
      file: include/libcamera/ipa/rkisp1.mojom
      start: interface IPARkISP1Interface {
      end: 'metadataReady(uint32 frame, libcamera.ControlList metadata);

        };'
      sha256: 32d657315ff8eac800254f0a5d39462d55bb4b0ef6ee5717930f3d3b8504162b
    statements:
    - text: IPU3와 RKISP1 인터페이스는 `init()`·`configure()`에서 센서 정보와 컨트롤 구성을 받고 반환 코드와 IPA 컨트롤 정보를 돌려줍니다. `mapBuffers()`와
        `unmapBuffers()`는 버퍼 목록과 ID를 전달하는 인터페이스입니다.
      evidence:
      - ipu3-api
      - rkisp1-api
      covers:
      - interface
    - text: 프레임별 `queueRequest()`·`computeParams()`·`processStats()`는 두 인터페이스 모두 async로 선언됩니다. 결과는 `paramsComputed`,
        `setSensorControls`, `metadataReady` 이벤트로 구분합니다. IPU3의 통계 입력에는 frameTimestamp가 있고 RKISP1의 파라미터 완료 이벤트에는
        bytesused가 있으므로 두 인터페이스를 같은 인자 목록으로 취급하면 안 됩니다.
      evidence:
      - ipu3-api
      - rkisp1-api
      covers:
      - interface
    - text: 인터페이스 선언만으로 프레임별 이벤트 도착 순서, 버퍼의 실제 매핑 수명과 하드웨어 적용 시점을 확정할 수 없습니다. 해당 IPA 구현과 파이프라인의 신호 연결을 대조하여 실행을
        확인해야 합니다.
      evidence:
      - ipu3-api
      - rkisp1-api
      kind: limitation
      covers:
      - interface
  - id: lifetime
    title: IPA 객체와 종료 순서
    question: 인터페이스를 만들고 비동기 처리를 끝내는 계약은 무엇입니까?
    sources:
    - &id015
      id: load
      file: src/libcamera/ipa_module.cpp
      start: \brief Load the IPA implementation factory
      end: \brief Verify if the IPA module matches
      sha256: a1e13344bfc27e191c6c24c682f3a088446d42c99b9497c6e831ab1b97b55619
    - *id012
    - id: thread-stop
      file: utils/codegen/ipc/generators/libcamera_templates/proxy_functions.tmpl
      start: '{%- macro stop_thread_body() -%}'
      end: '{%- endmacro -%}'
      sha256: 40789903a76fad82ef27bf4ecd2c79ed2c47d0e0ad9d6583fba12e9cf072884b
    - *id013
    statements:
    - text: '`IPAModule::load()`는 공유 객체에서 `ipaCreate` 심볼을 찾습니다. 적재에 성공한 유효 모듈만 `createInterface()`로 인터페이스를 만들 수
        있고, Threaded 프록시는 생성된 인터페이스를 unique_ptr로 소유합니다.'
      evidence:
      - load
      - threaded
      covers:
      - lifetime
    - text: Threaded 프록시의 `stop()`은 먼저 Stopping 상태의 재진입을 ASSERT로 검사합니다. 그 밖에 Running이 아니면 반환합니다. Running일 때는 Stopping으로 전환하고 Blocking 연결로 IPA stop을 호출한 다음
        스레드의 exit·wait를 수행합니다. 대기 중인 InvokeMessage를 전달한 뒤 Stopped로 바꿉니다.
      evidence:
      - thread-stop
      covers:
      - lifetime
    - text: Isolated 프록시의 소멸자는 IPC 연결이 있으면 Exit 메시지를 비동기로 보냅니다. 이 발췌만으로 worker 종료 확인이나 모든 자원 회수 완료를 보장하지는 않습니다.
      evidence:
      - isolated
      covers:
      - lifetime
    - text: worker 종료 대기, 비정상 종료 후 복구와 신호 재진입은 Process·worker 구현 및 실제 실행에서 확인해야 합니다.
      evidence:
      - isolated
      - thread-stop
      kind: limitation
      covers:
      - lifetime
  - id: errors
    title: 생성과 IPC 실패
    question: 모듈 생성과 IPC 오류는 어떻게 관찰됩니까?
    sources:
    - *id014
    - *id015
    - *id013
    - *id016
    statements:
    - text: 모듈 적재는 유효하지 않은 모듈, `dlopen()` 실패 또는 `ipaCreate` 심볼 조회 실패에서 false를 반환합니다. Isolated 프록시는 worker 경로가 없거나
        IPC 연결에 실패하면 valid 상태를 설정하지 않으며, 관리자는 유효하지 않은 프록시를 null로 반환합니다.
      evidence:
      - load
      - isolated
      - selection
      covers:
      - errors
    - text: 동기 IPC 호출은 응답을 기다리며 현재 스레드의 이벤트를 처리합니다. 2000ms 타임아웃이 끝나면 `-ETIMEDOUT`을 반환합니다. 전송 실패도 호출자에게 반환합니다. 생성
        프록시는 반환값이 있는 메서드에는 오류를 반환하고 void 메서드에서는 로그를 남긴 뒤 반환합니다.
      evidence:
      - ipc
      - isolated
      covers:
      - errors
    - text: 이벤트 처리 중 재진입과 worker 장애 시 파이프라인의 요청 취소·재시작 정책은 별도의 구현 검토와 실행 검증이 필요합니다.
      evidence:
      - ipc
      - isolated
      kind: limitation
      covers:
      - errors
  design_requirements:
  - id: selection
    question: 어떤 IPA 모듈과 실행 방식이 선택됩니까?
  - id: execution
    question: 동기 호출과 비동기 호출은 어디에서 실행됩니까?
  - id: interface
    question: 파이프라인과 알고리즘은 어떤 입력과 결과를 주고받습니까?
  - id: lifetime
    question: 인터페이스를 만들고 비동기 처리를 끝내는 계약은 무엇입니까?
  - id: errors
    question: 모듈 생성과 IPC 오류는 어떻게 관찰됩니까?
  show_evidence: true
- id: ipu3-lsc
  group: For Vendors
  title: IPU3 LSC와 상태 연결
  output: ipu3-lsc.md
  kind: prose
  lead: 현재 커밋의 LSC 구현과 IPU3 상태 연결을 확인하세요.
  reader: IPU3 변경을 검토하는 개발자
  semantic_review: true
  answers:
  - Lsc가 현재 facts에 존재합니까? 없으면 구현이 있다고 서술하지 마세요.
  - 직접 상속과 필드 참조를 구분하고 IPU3 상태의 LSC 연결을 설명하세요.
  - LSC 자유 함수의 역할은 추출된 근거 범위에서만 설명하세요.
  facts:
    classes:
    - libcamera::ipa::ipu3::algorithms::Lsc
    - libcamera::ipa::ipu3::IPAActiveState
    - libcamera::ipa::ipu3::IPAFrameContext
    - libcamera::ipa::lsc::ActiveState
    - libcamera::ipa::lsc::FrameContext
    - libcamera::ipa::LscAlgorithm
    - libcamera::ipa::ipu3::IPAContext
    - libcamera::ipa::ipu3::IPASessionConfiguration
    functions:
    - libcamera::ipa::ipu3::algorithms::quantize
    - libcamera::ipa::ipu3::algorithms::logCategoryIPU3Lsc
  watch:
  - src/ipa/ipu3/**
  - src/ipa/libipa/lsc*
  next:
    title: IPU3 LSC의 설정과 프레임 처리
    link: ipu3-lsc-flow.md
  narration: facts
- id: ipu3-lsc-flow
  group: For Vendors
  title: IPU3 LSC의 설정과 프레임 처리
  output: ipu3-lsc-flow.md
  kind: prose
  lead: LSC를 수정할 때는 초기 그리드 설정, 요청별 활성화 상태, 색온도에 따른 파라미터 갱신 조건을 차례로 확인하세요.
  reader: IPU3 렌즈 음영 보정의 동작을 확인하는 개발자
  semantic_review: true
  facts:
    classes:
    - libcamera::ipa::ipu3::algorithms::Lsc
    - libcamera::ipa::LscAlgorithm
    - libcamera::ipa::ipu3::IPAContext
    - libcamera::ipa::ipu3::IPAActiveState
    - libcamera::ipa::ipu3::IPAFrameContext
    - libcamera::ipa::lsc::ActiveState
    - libcamera::ipa::lsc::FrameContext
  watch:
  - src/ipa/ipu3/algorithms/lsc.*
  - src/ipa/ipu3/algorithms/meson.build
  - src/ipa/ipu3/ipa_context.*
  - src/ipa/libipa/lsc*
  next:
    title: 시스템 개요
    link: overview.md
  design_topics:
  - id: integration
    title: 빌드와 상태 연결
    question: LSC 구현 파일과 공유 상태 및 프레임별 상태는 어떻게 연결됩니까?
    sources:
    - id: build
      file: src/ipa/ipu3/algorithms/meson.build
      start: ipu3_ipa_algorithms = files([
      end: '])'
      sha256: 5b34e175668b4fedb33a633567f049c2738f6368c3b4d8ac26d069e77add82c9
    - id: context
      file: src/ipa/ipu3/ipa_context.h
      start: struct IPASessionConfiguration {
      end: '} /* namespace ipa::ipu3 */'
      sha256: e373aff7c5b5e5f034637b3d9030b0fdef974614261025791afd6e3146b1108d
    statements:
    - text: IPU3 알고리즘 빌드 목록에는 lsc.cpp가 포함됩니다. 이 목록에 있다는 사실만으로 실행 중인 카메라에서 LSC가 활성화됐다고 판단할 수는 없습니다.
      evidence:
      - build
      covers:
      - integration
    - text: IPAContext는 공유 activeState와 프레임 컨텍스트 큐를 보유합니다. IPAActiveState의 lsc 필드와 IPAFrameContext의 lsc 필드가 각각 공유
        상태와 프레임별 상태를 연결합니다.
      evidence:
      - context
      covers:
      - integration
  - id: setup
    title: 초기화와 크롭 설정
    question: 센서 크기에서 그리드를 만들고 크롭 설정을 전달하는 과정을 설명하세요.
    sources:
    - id: setup
      file: src/ipa/ipu3/algorithms/lsc.cpp
      start: static constexpr unsigned int kMaxNumHCells
      end: void Lsc::queueRequest(
      sha256: 943557ac46750b8b53d8280921b313e4331e786e8ff88766abb0fe02959d2f63
    statements:
    - text: init()는 센서 activeAreaSize를 기준으로 그리드를 계산합니다. 가로 73개와 세로 56개라는 셀 수 상한에 맞춰 필요한 셀 크기를 계산하고, 이를 2의 거듭제곱으로
        올림한 뒤 실제 셀 수와 블록 크기를 정합니다.
      evidence:
      - setup
      covers:
      - setup
    - text: 튜닝 데이터의 type이 polynomial인지 저장한 뒤, r·gr·gb·b 성분과 샘플 수 및 센서 크기를 공통 LSC 알고리즘의 init()에 전달합니다. 반환값은 호출자에게
        그대로 반환합니다.
      evidence:
      - setup
      covers:
      - setup
    - text: configure()는 analogCrop의 너비와 높이를 저장합니다. 각 축의 샘플 위치를 0부터 1까지 생성하고, 크롭 영역 및 공유 LSC 상태와 함께 공통 알고리즘의 configure()에
        전달합니다. 이 코드는 샘플 수가 1일 때의 나눗셈을 별도로 처리하지 않으므로 지원 센서 크기의 경계 조건은 추가 검증이 필요합니다.
      evidence:
      - setup
      covers:
      - setup
  - id: request
    title: 첫 프레임과 요청별 제어
    question: 첫 프레임의 기본 활성화 처리와 공통 알고리즘으로 넘기는 데이터를 설명하세요.
    sources:
    - id: request
      file: src/ipa/ipu3/algorithms/lsc.cpp
      start: void Lsc::queueRequest(
      end: static unsigned int quantize(
      sha256: 550c83f35d2a4d5218ac10731fe8650d8446e262841f2dd80a54e9238c7da287
    statements:
    - text: frame이 0이고 요청에 LensShadingCorrectionEnable 제어가 없으면 프레임의 enabled와 update를 true로 설정합니다. 소스 주석은 공통 알고리즘이
        알리는 기본값과 IPU3 드라이버의 기본 비활성 상태를 맞추기 위한 처리라고 설명합니다.
      evidence:
      - request
      covers:
      - request
    - text: 이후 공유 LSC 상태, 현재 프레임의 LSC 상태와 요청 controls를 공통 알고리즘의 queueRequest()에 전달합니다. 여기서 설명하는 것은 IPU3 래퍼의 전달 동작이며,
        모든 제어 값의 처리 규칙이나 콜백 실행 스레드까지 확인한 것은 아닙니다.
      evidence:
      - request
      covers:
      - request
  - id: prepare
    title: 파라미터 갱신 조건
    question: 색온도 변경에 따른 생략 조건과 커널에 전달하는 파라미터를 설명하세요.
    sources:
    - id: prepare
      file: src/ipa/ipu3/algorithms/lsc.cpp
      start: static unsigned int quantize(
      end: void Lsc::process(
      sha256: 1d11fc7397b377f682d4fab45d12dd20480e554176aae94ba2708a5f8f747268
    - id: constants
      file: src/ipa/ipu3/algorithms/lsc.cpp
      start: static constexpr unsigned int kMaxNumHCells
      end: int Lsc::init(
      sha256: 7807718b4ae9327f1ca428627d2b00f59ba38ced03261465ee8c79312ea27833
    statements:
    - text: prepare()는 현재 프레임 AWB의 색온도를 읽어 10 단위로 반올림합니다. update가 false일 때만 다음 생략 조건을 검사합니다. 비활성 상태이면 바로 반환하고, 활성
        상태에서도 직전 적용 색온도와의 차이가 5 미만이거나 양자화된 값이 같으면 갱신을 생략합니다. update가 true이면 이 생략 조건들을 적용하지 않습니다.
      evidence:
      - prepare
      - constants
      covers:
      - prepare
    - text: 갱신 경로에서는 use.acc_shd를 1로 설정하고 shd_enable에 현재 활성 상태를 기록합니다. update가 true이고 enabled가 false인 경우에도 이 플래그를
        기록한 뒤 반환하므로, 단순히 비활성 상태의 모든 호출을 생략하는 것은 아닙니다.
      evidence:
      - prepare
      covers:
      - prepare
    - text: 활성 상태에서는 그리드와 크롭 오프셋을 설정하고, 양자화한 색온도로 interpolateComponents()를 호출합니다. 반환된 r·gr·gb·b 배열을 LUT에 복사한 뒤 마지막으로
        적용한 색온도 두 값을 저장합니다. 실제 하드웨어의 적용 시점과 LUT 분할 경계의 적합성은 기기에서 별도로 검증해야 합니다.
      evidence:
      - prepare
      covers:
      - prepare
  - id: metadata
    title: 메타데이터와 확인 범위
    question: 프레임 처리 결과를 전달하는 경계와 미검증 사항을 설명하세요.
    sources:
    - id: process
      file: src/ipa/ipu3/algorithms/lsc.cpp
      start: void Lsc::process(
      end: REGISTER_IPA_ALGORITHM(Lsc, "Lsc")
      sha256: e1411dc6710e62f774ea84588792a3f6fd547beb3442bc7cc49495eb19dff05e
    statements:
    - text: IPU3의 process()는 프레임 LSC 상태와 출력 metadata를 공통 알고리즘의 process()에 전달합니다. 이 래퍼는 전달받은 통계를 직접 읽지 않습니다. 파일 끝에서는
        Lsc라는 이름으로 알고리즘을 등록합니다.
      evidence:
      - process
      covers:
      - metadata
    - text: 이 문서는 고정된 libcamera 커밋의 구현을 설명합니다. 센서별 화질, 실행 스레드, 실제 프레임의 파라미터 적용 시점과 사내 Camera HAL 동작을 검증한 결과는 아닙니다.
      evidence:
      - process
      covers:
      - metadata
  design_requirements:
  - id: integration
    question: LSC 구현 파일과 공유 상태 및 프레임별 상태는 어떻게 연결됩니까?
  - id: setup
    question: 센서 크기에서 그리드를 만들고 크롭 설정을 전달하는 과정을 설명하세요.
  - id: request
    question: 첫 프레임의 기본 활성화 처리와 공통 알고리즘으로 넘기는 데이터를 설명하세요.
  - id: prepare
    question: 색온도 변경에 따른 생략 조건과 커널에 전달하는 파라미터를 설명하세요.
  - id: metadata
    question: 프레임 처리 결과를 전달하는 경계와 미검증 사항을 설명하세요.
  show_evidence: true
