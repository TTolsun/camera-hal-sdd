# 프로젝트 목적과 작업 기준

최종 적용 대상은 **사내 Camera HAL**입니다. libcamera는 집에서 파이프라인을 검증하기 위한 공개 테스트 대상이며, 제품의 최종 대상이 아닙니다.

- `camera-hal-sdd-generator`는 사실 추출, 변경 영향 분석, 문서·Mermaid 생성, 검증과 사이트 구성 기술을 관리합니다. 사내 HAL에 재사용할 공통 기능은 이 저장소에 구현합니다.
- `TTolsun/libcamera-sdd`는 공개 libcamera upstream 이력 보관과 테스트 문서 배포용입니다. 사내 소스나 사내 소스에서 파생된 facts·문서·프롬프트·로그를 이 공개 저장소에 배포하지 않습니다.
- libcamera 클래스, 경로, Meson 빌드와 GitHub Pages 설정은 예제 설정으로 분리합니다. 사내 HAL의 빌드 시스템, 저장소 호스트, 코드 링크 형식과 게시 방식이 동일하다고 가정하지 않습니다.
- 사내 환경의 LLM, 네트워크와 정적 자산 제공 방식을 설정으로 교체할 수 있게 합니다. 공개 CDN이나 GitHub 서비스가 최종 운영의 필수 의존성이 되지 않도록 설계합니다.
- 사내 HAL의 실제 구성과 소스가 제공되기 전에는 사내 적용이나 검증이 완료되었다고 표현하지 않습니다. libcamera 테스트 결과와 사내 HAL 검증 결과를 구분합니다.
- 변경 시 반복 생성과 유지보수를 우선합니다. 문서 디자인은 `DESIGN.md`, 실행·검증 절차는 `docs/document-site.md`를 따릅니다.
- 사용자의 상시 지시에 따라 이 프로젝트의 변경 작업에는 공개 libcamera SDD 갱신도 포함합니다. 생성기 변경을 리뷰·검증·병합하고, 공개 libcamera 입력으로 필요한 추출·원고 생성·사이트 빌드를 수행한 뒤 `TTolsun/libcamera-sdd`에 PR로 반영·병합하여 GitHub Pages 배포와 공개 URL을 확인합니다. 이 범위의 병합·공개 배포는 매번 다시 허락을 묻지 않습니다. 사내 데이터 배포 금지와 자동 검사·사람 승인 구분은 그대로 지킵니다.
- 작업 완료는 생성기 병합, 공개 SDD 반영, Pages 배포와 공개 산출물 확인까지 구분해 보고합니다. 지침·테스트만 변경되어 공개 산출물에 차이가 없으면 불필요한 배포 커밋을 만들지 않고 확인 결과와 배포 불필요 사유를 알립니다. 배포가 막히면 로컬 생성만으로 완료했다고 하지 말고 원인과 남은 단계를 명시합니다.
- 생성된 HTML을 직접 수정해서 디자인을 유지하지 않습니다. 디자인 변경은 `DESIGN.md`의 계약(레이아웃, 그림 확대·검색 동작, 실패 처리)을 따르고 생성기 코드에서 수행합니다.
- CLI 사용법과 `impact.json`의 schema 1 형식은 유지합니다.
- 노드 한도 초과나 서로 다른 커밋의 facts·원고를 조용히 통과시키지 않고, 작업을 실패시키며 이유를 알립니다.
- 생성·배포하는 문서 원고는 `style/README.md`의 집필 규칙(i-have-adhd + fluent-korean 번안)을 따릅니다. 이 규칙 파일이 바뀌면 원고 해시가 달라지므로 기존 원고를 다시 검토해야 합니다.
